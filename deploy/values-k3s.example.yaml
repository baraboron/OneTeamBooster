# Discovered registry host; the oneteambooster project and tags are PROPOSED, not verified.
frontend:
  image:
    repository: wonix-ops.ips.co.kr/oneteambooster/frontend
    tag: "0.1.0"
backend:
  image:
    repository: wonix-ops.ips.co.kr/oneteambooster/backend
    tag: "0.1.0"

# Populate only after a pull credential exists in the release namespace.
imagePullSecrets: []
# - name: otb-harbor-pull

# Leave disabled until hostname/DNS/TLS and access scope are agreed.
ingress:
  enabled: false
  className: nginx
  host: ""
  tlsSecretName: ""
