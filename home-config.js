// Fill only after the authenticated, company-wide points API is available.
// Same-origin URLs only. Never put credentials or employee data here.
window.OTBHomeConfig=Object.freeze({
  leaderboardUrl:'',
  leaderboardEventsUrl:'',
  refreshMs:15000
});
