# 기능 점검 및 후속 과제

## 2026-09-22 관리자 전체 칭찬 이력 · 로컬 반영

- `administrator_grants`에 명시적으로 등록된 사용자만 서버가 관리자 권한으로 인정한다. 직책·이름·클라이언트 헤더 값으로 권한을 추정하지 않는다.
- 관리자에게만 왼쪽 메뉴를 표시하고, 전체 사용자의 칭찬 이력을 50건 단위 표로 조회한다. 사용자·받은/보낸 구분·이름/부서/업무/내용 검색 필터를 제공한다.
- 권한 부여·해제는 서버 운영자 스크립트의 `grant-admin <USER_ID>` / `revoke-admin <USER_ID>`로만 수행한다. 웹 화면에는 권한 변경 기능이 없다.
- 사용자 요청에 따라 테스트 프리뷰의 명시적 관리자 이름을 `송재현`으로 설정했다. 서버 시작 시 두 승인된 테스트 사용자가 각각 정확히 1명인지 확인한 뒤 송재현의 현재 `USER_ID`에 관리자 권한을 저장한다.
- 실제 로그인은 여전히 미연결이다. 테스트 사용자 모드에서의 사용자 선택은 인증이 아니므로 운영 관리자 권한으로 간주하지 않는다. 배포는 별도 확인이 필요하다.

## 2026-09-21 소속 구분·어투 및 홈 요약 카드 · 로컬 반영

- 부스터 작성에 같은 팀/그룹·타 팀/그룹 선택을 추가했다. 관계와 함께 AI 입력에 전달하며 여섯 조합의 존댓말·친밀도 규칙을 서버/게이트웨이에 동일하게 적용했다. 로컬 템플릿 시연도 같은 기준으로 조절한다.
- 소속 선택값은 저장·수신/발신 카드·재사용까지 보존한다. 기존 기록은 미입력으로 유지하며 임의 추정하지 않는다. 기존 타팀 발신 응원 집계에도 연결된다.
- 운영 원칙 문구를 요청한 두 줄로 수정했다. 홈 요약 카드는 큰 상단 공백을 제거하고 응원 문구 왼쪽·포인트 오른쪽으로 배치하며 작은 화면에서는 세로로 전환한다.
- 자동 검사 55개, 임시 PostgreSQL 통합 검사 2개 통과. 컬럼 추가/반복 마이그레이션, 기존 기록과 중복 방지, 신규 소속 저장/조회, 여섯 조합의 AI 전송 경로를 검증했다. 원격 임시 컨테이너·네트워크·이미지·소스 폴더는 정리했다.
- 브라우저/실제 AI 생성 검증 및 배포는 미실행이다. 게이트웨이 `praise-ko-v2`와 사내 앱은 함께 업데이트해야 한다.

## 2026-09-21 협업 분석 및 발신자 소속 · 로컬 반영

- 홈 전사 포인트 TOP 10을 제거하고 협업 분석으로 통합했다. 홈에서 순위 클라이언트를 로드하지 않으며 테스트 사용자 기록 갱신에서도 순위 API를 요청하지 않는다. 기존 서버 API와 포인트 원장은 유지한다.
- 내가 받은 칭찬만 관계(OneTeam Partner), 업무(Mission Record), 협업역량(Boost Point), 변화(OneTeam Impact)로 분석한다. 상세 분석은 네 항목의 설명·건수·비율과 누락 건수를 표시한다.
- 복수 선택과 2·3순위를 포함하며 한 칭찬의 같은 항목은 한 번만 집계한다. 관계는 발신자의 선택값을 그대로 사용한다. 시연/테스트 범위와 연결 오류를 안내한다.
- 홈의 받은 칭찬 제목에 발신자 소속을 추가했다. 소속이 없으면 ‘소속 정보 없음’을 표시한다.
- 검증: 홈·앱 초기화·디자인·문구·리더 범위·테스트 사용자 자동 검사 26개 통과. 브라우저 검증과 배포는 수행하지 않았다. 아래 절은 이전 구현 이력이다.

## 2026-09-21 14:17 KST Supabase AI 게이트웨이 · 배포 완료

- 사내 `/api/drafts`가 전용 토큰으로 Supabase 게이트웨이를 호출한다. 모델 `gpt-5.6-luna`가 선택값에 맞춘 초안을 생성하며, 사용자가 수정 후 별도로 전송한다. 생성만으로 포인트나 칭찬 기록은 바뀌지 않는다.
- 게이트웨이 URL은 Helm 설정, 토큰은 `otb-runtime` Secret으로 주입했다. OpenAI 키는 Supabase Secrets에만 있다. 실패 시 입력을 유지하고 오류를 안내하며 템플릿을 실제 AI 결과로 표시하지 않는다.
- 최종 Helm 리비전 11. 로컬 57개·임시 PostgreSQL 통합 2개와 실제 앱 경로 생성 검증 통과. 브라우저에서 테스트 사용자 선택→가상 업무 입력→실제 초안 생성→수정까지 확인하고 전송하지 않았다.
- 최초 연동은 수동 배포했고, 이후 사내 앱 업데이트는 main의 게이트웨이 설정을 사용해 Jenkins가 배포한다. 상세 증거는 `deploy/OPENAI_GATEWAY.md` 참고.
- 실사용 로그인·리더 권한은 계속 미완료다. 아래 절은 당시의 구현 상태를 기록한 이력이다.

## 2026-09-21 테스트 사용자 연동 · 배포 완료

- 사용자가 송재현·김영훈 선택 방식의 테스트 사용자 모드를 요청했다. 두 이름은 현재 HR API에서 각각 1건으로 확인했다. ID는 코드에 고정하지 않고 서버 디렉터리에서 확인하며, 누락/동명이인 발생 시 테스트 모드 시작을 중단한다.
- 테스트 사용자 선택 → 임직원 검색/부서 필터 → 초안 수정 → 칭찬 저장 → 사용자 전환 → 수신함/답장 → 포인트/홈 요약/테스트 TOP 10을 서버 API에 연결했다. 15초마다 기록을 확인하며 전송·답장 직후 다시 읽는다.
- 테스트 기록은 PostgreSQL `otb_preview` 스키마에 분리한다. 실제 사용자 인증은 없으며 서버는 지정한 두 사용자만 발신자로 허용한다. 같은 사이트의 Origin 검증, 요청 ID 기반 중복 지급 방지, 사용자별 조회, 자기 자신 전송 방지를 적용한다.
- 실사용 로그인/리더 권한/실제 AI 모델 생성은 구현하지 않았다. 리더 조회는 테스트 모드에서 차단한다. 메시지는 기존 편집 가능한 템플릿 초안이며 테스트 기록을 운영 순위로 표시하지 않는다.
- 프런트엔드/도메인 30개, 백엔드 12개, 임시 PostgreSQL 통합 2개 통과. Docker·Helm·HTTP 검증도 통과했다. 실제 브라우저 검증은 미실행.
- 사용자 명시적 승인 후 2026-09-21 11:05 KST에 Helm 리비전 3으로 배포했다. 프런트엔드/백엔드 모두 Ready. 실제 HTTP에서 두 사용자 선택·사용자별 조회·검색·183개 부서·테스트 순위·접근 제한을 확인했다. 배포 확인 중 칭찬 기록은 만들지 않았다.

## 2026-09-21 임직원 검색 구현 · 배포 전

- 이름·부서·이메일·직책·ID 검색, 부서 필터, 20명씩 더 보기와 동명이인 선택을 구현했다. 원래 대문자 필드명과 INSERT_DT를 보존한다.
- 서버는 기본적으로 인증을 요구한다. `backend.previewEmployeeSearch=true`는 사내 시연에서 검색 GET 두 경로만 허용하는 명시적 선택이다. 현재 값은 false이며 검색 공개 방식에 대한 사용자 답변 전 배포하지 않았다.
- 실제 직원 선택은 메모리 안의 초안 작성에만 사용한다. 실제 전송·답장·포인트·리더 조회는 기존 인증 차단을 유지한다. API 키는 서버 환경변수에만 전달한다.
- 로컬 선택 배열과 2·3순위의 저장·재사용·카드 표시·통계를 수정했다. 기존 기록에 없는 순위는 복원하지 않는다.
- 실제 HR 확인: 1,501명·183부서, 중복 ID/빈 이메일/빈 부서 0. 아래 9월 18일 수치는 당시 결과다.
- 검증 증거 및 현재 배포와의 차이는 `deploy/VALIDATION.md`에 기록한다. 실제 AI 생성과 인증된 업무 UI 연결은 후속 과제다.

## 현재 상태 · 2026-09-18 인사 연동 및 프리뷰 배포

- 접속: `http://192.168.20.72:30081`. 프런트엔드·백엔드·PostgreSQL이 Helm으로 배포되어 있다.
- 인사 API 전체 페이지 동기화(실확인 1,504명/183부서), 필드명 보존, 키 비공개, 실패 시 기존 디렉터리 보존을 구현했다.
- 칭찬·답장 영속 저장, 발신 10P/수신 20P/답장자 5P 트랜잭션, 중복 방지, 한국시간 발송 한도, 2·3순위 배열 보존, 전사 공동 순위, 리더 담당 부서 필터를 서버에 구현하고 가상 데이터로 DB 통합 검증했다.
- 사용자 요청으로 **로그인은 추후 구현**한다. 공개 화면은 LocalStorage 시연이며 실제 업무 API는 모두 401로 보호된다. UI에서 실제 직원 선택·전송·답장을 활성화한 상태는 아니다.
- 인증 연동 후 `backend/application.js`에 검증된 사용자 식별/권한·CSRF 처리를 연결하고 `api-client.js`를 사용해 UI 저장/조회를 서버로 전환해야 한다. 직책명으로 리더 권한을 자동 부여하지 않는다.
- 실제 AI 생성, 조직도 계층, 단체 발송, 운영 DB 백업·고가용성은 후속 과제다.
- 검증: 로컬 32개, 컨테이너 10개, 가상 PostgreSQL 통합 1개 통과. 배포 HTTP/401 차단 및 브라우저 초안·시연 저장·새로고침 유지 확인. 아래 항목은 이전 시점 기록이며 이 절의 현재 상태를 우선한다.

## K3s 배포 준비 · 2026-09-18

- 기존 정적 프런트엔드의 Docker 이미지와 Node.js API 기본 서버, Helm 차트를 추가했다. 상세 상태와 설치 절차는 `deploy/README.md` 참고.
- API 기본 서버는 health/readiness와 `/api/system`만 제공한다. 로그인·DB·전달·답장·원장·AI 생성·전사 랭킹은 여전히 미구현이며 업무 API는 503으로 응답한다.
- 프런트엔드의 LocalStorage, 시연 구분, 비어 있는 순위 API 설정은 유지한다. 이번 작업을 운영 기능 연결 완료로 간주하지 않는다.

## 이번 반영: 리더 칭찬 기록 조회 (로컬 시제품)

- 역할 전환 시연: 그룹원은 리더 메뉴/내용 숨김, 리더는 담당 그룹 현황 접근.
- 그룹원별 받은/보낸 칭찬과 칭찬 원문을 조회. 0건 그룹원도 표시.
- 그룹원·수신/발신 필터와 협업이 많은 부서 TOP 3을 제공.
- 리더는 담당 그룹의 칭찬만 조회하며, 리뷰·추가점수·지급 이력은 제공하지 않음.

## PDF 기준 누락 및 구현 차이

근거: 캠페인 기획안 4쪽(진행 프로세스), 5쪽(선택문항 상세), 6쪽(실행방안).

| 우선순위 | 항목 | 현재 상태 / 다음 구현 |
| --- | --- | --- |
| 운영 전 필수 | 사내 로그인·조직도·리더 권한 | 브라우저 역할 전환은 시연용. 서버 세션과 그룹별 API 조회/쓰기 검증, DB 접근 정책 필요. 실제 인사 데이터를 넣지 않을 것. |
| 운영 전 필수 | 포인트 원장과 동시성 | 발신 10P/수신 20P와 답장자 5P의 실제 계정별 원장, DB 트랜잭션 및 칭찬별 고유 제약 필요. 로컬 저장만으로 동시 지급을 보장하지 않음. |
| 높음 | 선택값 완전 보존 | Boost Point/Impact 2·3순위가 전송 데이터에서 누락됨. 순위 배열 저장, 재사용 복원, 분석 집계까지 연결 필요. |
| 높음 | 실제 메시지 생성 | AI 호출 없이 템플릿 생성 중. 관계별 톤과 선택 근거를 반영하는 생성 서비스 연결 필요. |
| 높음 | 실시간 수신·답장·알림 | 발송/수신 데이터가 별도 로컬 목록. 실제 상대 계정 전달, 답장 본문, 알림 및 읽음 확인 필요. |
| 높음 | 단체 발송 | 관계 옵션만 제공. 조직도 기반 다중 수신자, 개인별 전달·보상·중복 방지 필요. |
| 연결 대기 | 협업 분석 및 랭킹 | 고정 개인 랭킹 제거. 전사 TOP 10 UI·읽기 API·SSE/15초 재조회 클라이언트 구현. 실제 전사 원장·인증 API 미제공으로 운영 데이터 연결은 미완료. |
| 보통 | 조직별 커스터마이징 | 선택항목·보상·캠페인 기간 설정 및 파일럿 중간 리뷰/우수 격려 사례 선정 기능 필요. |
| 반영 / 화면 검증 대기 | 모바일 메뉴 | 760px 이하에서도 가로 메뉴·역할 선택을 유지하도록 개편. 실제 모바일 브라우저 검증은 후속 확인 필요. |

## 정책 확인이 필요한 부분

- 리더 리뷰·추가점수는 이번 시제품 범위에서 제외했다. 리더 화면은 담당 그룹의 칭찬 기록 조회만 제공한다.
- 발송 제한은 하루 최대 3회, 동일 수신자 월요일 기준 주 1회로 적용한다. 현재 브라우저의 전송 기록으로 판정하므로 운영 환경에서는 서버 시간·계정별 원장으로 검증해야 한다.
- 그룹 현황은 날짜가 없는 기존 기록을 임의로 월별 집계하지 않고 전체 기록 기준으로 표시.
- 기존 브라우저에 저장된 리더 리뷰 원장은 삭제하거나 이관하지 않는다. 화면과 점수 계산에서는 사용하지 않는다.

## 검증

`node --test leader-domain.test.js`: 리더 역할과 담당 그룹 범위의 칭찬 조회 검증.

브라우저 확인: 그룹원 모드 리더 메뉴 숨김, 리더 전환/그룹원 조회, 0건 빈 상태, 부서 TOP 3 및 수신/발신 필터는 이번 변경 후 확인 필요.

검증 중 발견한 기존 초기화 오류(잘못된 ID 선택자, 단일 요소에 forEach 호출)도 수정해 홈 수치와 목록이 정상 표시되는 것을 확인함.

## 디자인 체계 개편 · 2026-09-07

- 공식 사례 조사 및 DESIGN_GUIDELINES.md 작성, AGENTS.md 필수 적용 규칙 연결.
- 홈의 칭찬 피드 우선 배치, 화면별 카드/타이포/입력/리더 상태 통일, 단일 CSS 토큰 체계로 정리.
- 고정 랭킹에 ‘예시’, 생성 안내에 ‘템플릿 기반 시연’을 표시. 기간이 없는 집계는 전체 기록으로 설명.
- 키보드 포커스, 팝업 Tab/Escape/복귀 처리, aria-current/aria-pressed, 모바일 메뉴 및 동작 감소 설정 구현.
- 자동 검사 9건 통과: 리더 정책 3건 + 정적 디자인/경로/대비/접근성 연결 6건.
- 위 브라우저 확인 기록은 리더 기능 추가 시점의 결과다. 이번 디자인 변경 후 브라우저 시각·인터랙션 검증은 아직 수행하지 않았으며, 정적 검사로 대체 완료 처리하지 않는다.

## 맞춤 응원·TOP 10·색상·문구 개선 · 2026-09-07

- 개인 상태 7종, 제목/기록/마무리 98개 조각, 672개 조합. 상태 변화 즉시 갱신 및 ‘다른 응원 보기’. 자동 타이머 회전 없음.
- TOP 10 기본 화면은 연결 대기. 사용자가 시연을 켜면 가상 점수와 내 로컬 점수로 재정렬. 실제 전사 순위로 표시하지 않음.
- 읽기 API, SSE 변경 알림, 15초 재조회, 탭 비활성 중단, 오류/지난 집계 표시 구현. 연결 계약은 LEADERBOARD_API.md에 기록.
- 받은 칭찬 오렌지, 보낸 칭찬/연결 틸, 리더 바이올렛, 완료 그린, 스포트라이트 앰버를 작은 요소에 적용. 공식 이미지는 그대로 유지.
- 홈/수신·발신/리더/리포트 안내와 응원 문구를 일상어로 교정. 생성 초안은 선택 항목을 자연스러운 문장으로 변환. 저장된 메시지와 PDF 선택 항목은 변경하지 않음.
- 칭찬 요약에서 한 업무의 건수처럼 전체 수신 건수를 표시하던 표현을 수정. 동점 강점은 함께 표시.
- 이전 변경 시 전체 22개 자동 검사를 통과했다. 이번 리더 기능 축소 후에는 정적 디자인·범위·672조합·순위/통신 모의·앱 초기화·문구 검증을 다시 실행한다. 실브라우저 및 운영 API 검증은 미수행.
