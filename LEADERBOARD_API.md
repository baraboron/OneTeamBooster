# 전사 TOP 10 데이터 연결 계약

## 현재 상태

홈 UI·정렬·시연·자동 갱신 클라이언트는 구현했다. 이 저장소에는 전사 포인트 DB, 로그인 세션, 운영 API가 없다. 따라서 기본 상태는 **연결 대기**이며 실제 전사 실시간 순위는 아직 제공하지 않는다.

home-config.js의 leaderboardUrl과 leaderboardEventsUrl은 현재 빈 문자열이다. 빈 설정에서는 네트워크 요청과 폴링을 하지 않는다. 연결 준비에 필요한 정보는 전사 포인트 원본, 인증 방식, 집계 기간, 승인된 표시명/조직 범위다.

## 읽기 API

같은 사이트의 인증된 GET 엔드포인트를 leaderboardUrl로 설정한다. 경로는 운영 API가 준비된 뒤 넣는다. URL이나 파일에 API 키를 넣지 않는다.

응답 예시(아래 인물·점수는 가상):

```json
{
  "scope": "company",
  "mode": "live",
  "periodLabel": "2026 OneTeam 캠페인 누적",
  "asOf": "2026-09-07T06:00:00.000Z",
  "leaders": [
    {"userId":"example-a","displayName":"예시 A","groupName":"예시 그룹","points":150,"rank":1},
    {"userId":"example-b","displayName":"예시 B","groupName":"예시 그룹","points":125,"rank":2},
    {"userId":"example-c","displayName":"예시 C","groupName":"예시 그룹","points":100,"rank":3}
  ]
}
```

- 서버가 캠페인 전사 원장의 합계로 집계한다. 개인 브라우저에서 보낸 points/identity를 그대로 신뢰하지 않는다.
- 정렬: 점수 내림차순. 공동 순위는 competition ranking(100,100,80 → 1,1,3).
- rank <= 10의 모든 사람을 반환한다. 공동 10위가 여러 명이면 모두 표시한다. 동점자를 임의로 탈락시키지 않는다.
- 동점 내부는 userId로 안정 정렬한다. 이 순서는 우열이 아니다.
- 10위까지 기록이 없으면 있는 기록만 반환한다. 빈 원장은 leaders:[]로 응답한다.
- points는 0 이상의 안전한 정수, userId는 중복 없는 문자열, displayName은 비어 있지 않은 문자열이다.
- 서버는 표시가 승인된 최소 정보만 반환한다. 전체 직원 목록, 이메일, 불필요한 개인정보를 내려주지 않는다.
- asOf는 조회 시각이 아니라 **점수 집계 기준 시각**이다. periodLabel에 캠페인/기간을 명시한다.
- 중복 ID, 잘못된 순위, 음수/문자열 점수, 비전사 범위, 잘못된 시각은 클라이언트에서 거부한다.
- 사용자에게 승인된 전사 캠페인 보기 권한을 서버가 검증한다. 401/403 응답을 로그인 성공처럼 숨기지 않는다.

## 갱신 방식

- 설정 후 첫 조회, 기본 15초 재조회, 탭 복귀, 온라인 복구 시 다시 조회한다.
- leaderboardEventsUrl이 준비되면 같은 출처의 SSE(EventSource)를 연결한다. 서버는 점수 원장 커밋 후 data 이벤트로 변경을 알린다.
- 이벤트 데이터 자체를 순위로 사용하지 않고 읽기 API를 재조회한다. 브라우저 로컬 쓰기로 전사 점수를 변경하지 않는다.
- SSE 장애 시 15초 폴링을 유지한다. 폴링만 작동하면 ‘자동 갱신’, SSE 연결과 최신 집계가 확인되면 ‘실시간 연결’로 표시한다.
- 요청 제한 8초, 중복 요청 억제, 중단된 요청의 늦은 응답 무시. 탭이 숨겨지면 폴링/SSE를 중단한다.
- 조회 실패 시 마지막 성공 결과를 유지하되 연결 오류와 집계 시각을 노출한다. 실패를 0점/빈 원장으로 바꾸지 않는다.
- 집계가 60초를 넘으면 ‘지난 집계’로 구분한다. 네트워크·서버 지연이 있어 수신 즉시 화면 반영을 절대 보장하는 구조는 아니다.

## 시연

사용자가 ‘시연 순위 보기’를 눌렀을 때만 가상 구성원 150~65P와 내 로컬 포인트를 함께 정렬한다. 칭찬 전송·리더 보상으로 내 점수가 바뀌면 재정렬된다.

시연 상태와 안내는 항상 유지한다. 시연 진입/종료는 전사 서버나 로컬 포인트 원장을 수정하지 않는다. 자동 재생·가상 점수 증가는 없다.

## 검증 범위

home.test.js: 순위/동점, 계약 검증, 정상 조회/오류/오래된 결과, SSE 갱신/폴링 전환, 중복 요청·중단 응답, 미연결 시 무요청을 모의 전송 계층으로 검사.

app-boot.test.js: 모의 DOM에서 전체 초기화, 동기부여 상태 변화, 지급 후 시연 순위 반영을 검사.

운영 API 및 실브라우저 검증은 아직 수행하지 않았다. 연결 주소와 인증 방식이 확보되어야 실제 전사 기능 검증을 진행할 수 있다.
